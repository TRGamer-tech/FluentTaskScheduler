<ContentDialog
    x:Class="FluentTaskScheduler.Dialogs.XmlEditorDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Edit Task XML"
    PrimaryButtonText="Save"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    MinWidth="800"
    MaxWidth="1000">

    <ContentDialog.Resources>
         <x:Double x:Key="ContentDialogMaxWidth">2000</x:Double>
    </ContentDialog.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="WARNING: Editing raw XML can break the task. Use with caution." 
                   Foreground="Orange" Margin="0,0,0,12" FontWeight="SemiBold"/>

        <TextBox x:Name="XmlTextBox" 
                 Grid.Row="1"
                 AcceptsReturn="True" 
                 TextWrapping="NoWrap"
                 FontFamily="Consolas"
                 MinHeight="400"
                 MaxHeight="600"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"/>
    </Grid>
</ContentDialog>
